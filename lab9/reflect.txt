In this workshop, I used the thread class to create two threads that would run the converter function. This was accomplished by
using std::bind to bind the function object with the variables we pass into std::bind to simulate a function call